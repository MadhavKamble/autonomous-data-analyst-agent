# Base URL of the FastAPI backend. Defaults to the local dev server if unset.
# At deployment, set this to the Render service URL, e.g.:
# VITE_API_BASE_URL=https://adaa-backend.onrender.com
VITE_API_BASE_URL=http://localhost:8000
